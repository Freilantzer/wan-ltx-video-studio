[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "wan-ltx-video-studio"
version = "0.1.0"
description = "Local-first WAN/LTX video generation studio."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
